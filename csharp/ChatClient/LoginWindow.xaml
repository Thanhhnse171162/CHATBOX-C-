<Window x:Class="ChatClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat Group — Sign In" Height="520" Width="440"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="24">
    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource LineBrush}" BorderThickness="1"
            CornerRadius="{StaticResource CardRadius}" Padding="28"
            MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,18">
          <TextBlock Text="Chat Group" FontSize="18" FontWeight="SemiBold"/>
          <TextBlock Text="Đăng nhập / đăng ký để vào phòng chat" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Form -->
        <StackPanel Grid.Row="1">
          <TextBlock Text="TCP host:port" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
          <TextBox x:Name="HostBox" Text="localhost:5000" Margin="0,0,0,12"/>

          <TextBlock Text="HTTP port" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
          <TextBox x:Name="HttpPortBox" Text="5001" Margin="0,0,0,12"/>

          <TextBlock Text="Username" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
          <TextBox x:Name="UserBox" Margin="0,0,0,12"/>

          <TextBlock Text="Password" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
          <PasswordBox x:Name="PassBox" Margin="0,0,0,10"/>

          <TextBlock x:Name="StatusText" Foreground="{StaticResource DangerBrush}" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Margin="0,6,0,0">
          <Button Content="Join Chat" Click="Join_Click"
                  Background="{StaticResource PrimaryBrush}" Foreground="White"
                  BorderThickness="0" FontWeight="SemiBold"/>
          <Button Content="Register" Click="Register_Click"
                  Background="Transparent" BorderThickness="0" Foreground="{StaticResource PrimaryBrush}"
                  Margin="0,8,0,0" Padding="10,6"/>
        </StackPanel>

        <!-- Helper text -->
        <Border Grid.Row="3" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,16,0,0">
          <TextBlock Foreground="{StaticResource MutedBrush}">
            host:port (TCP) — ví dụ: localhost:5000
          </TextBlock>
        </Border>
      </Grid>
    </Border>
  </Grid>
</Window>
